version: "2"

linters:
  enable:
    - bodyclose
    - misspell
    - unconvert
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - bodyclose

formatters:
  enable:
    - gofmt
